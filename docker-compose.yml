version: '3'

services:
  api:
    container_name: cicd-template
    image: thutgtz/cicd-template:dev
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    command: npm test